# Skill: classify_claim

Given the structured claim, classify into one of:

- `auto.collision`
- `auto.theft`
- `home.water_damage`
- `home.fire`
- `health.routine`
- `health.emergency`

Return a JSON object with `claim_class` and `confidence`.
