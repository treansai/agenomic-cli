# Skill: compensation_reasoning

Given a classified claim and the relevant policy section, produce a
recommended `compensation_amount` (number, USD), a `policy_source`
identifier matching the policy book, and a brief `reasoning` field.
